import { getPayloadHMR } from '@payloadcms/next/utilities'
import configPromise from '@payload-config'
import { calculateAverageStability } from './flashcardHelpers'

export async function checkLessonProgress(reviewedCards: any[]) {
  const results: any[] = []

  return results
}
